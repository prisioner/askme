.content-wrapper.buffer-both.text-center
  .buffer-both
    h1.title-medium = t('pages.users.index.title')

  - @users.each do |user|
    .users-list-item
      .user-image-div.user-image-div-small style="border-color: #{user_border_color(user)}"
        = link_to image_tag(user_avatar(user), class: 'user-image', title: user_nickname(user)), user_path(user)
